# Frontend Environment Variables

# API Configuration
# Base URL for the Quiz Builder backend API
# This URL is exposed to the browser, so it should be accessible from the client
NEXT_PUBLIC_API_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
